# UI Tweaks Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tres ajustes visuales: reducir logo Hero a 400px, quitar prefijos numéricos de todas las secciones, y reemplazar botón "Reservar cita" de Encuéntranos por "Cómo llegar" enlazando a Google Maps.

**Architecture:** Cambios puramente visuales en componentes existentes. Sin nuevas abstracciones ni dependencias. Cada tarea toca un único archivo.

**Tech Stack:** React, Tailwind CSS, Vite + vite-plugin-singlefile

---

## Archivos afectados

| Archivo | Cambio |
|---|---|
| `app/src/components/Hero.tsx:27` | `iconSize={500}` → `iconSize={400}` |
| `app/src/components/Services.tsx:56` | Quitar `01 · ` del label |
| `app/src/components/Team.tsx:45` | Quitar `02 · ` del label |
| `app/src/components/Gallery.tsx:45` | Quitar `03 · ` del label |
| `app/src/components/Booking.tsx:12` | Quitar `04 · ` del label |
| `app/src/components/Reviews.tsx:42` | Quitar `05 · ` del label |
| `app/src/components/Contact.tsx:13` | Quitar `06 · ` del label |
| `app/src/components/Contact.tsx:83-85` | Reemplazar botón Reservar → Cómo llegar → `MAPS_URL` |

---

## Task 1: Reducir logo en Hero

**Files:**
- Modify: `app/src/components/Hero.tsx:27`

- [ ] **Step 1: Editar iconSize**

En `Hero.tsx` línea 27, cambiar:
```tsx
<LogoMark iconSize={500} className="mb-8" />
```
por:
```tsx
<LogoMark iconSize={400} className="mb-8" />
```

- [ ] **Step 2: Verificar en dev server**

```bash
cd app && npm run dev
```
Abrir el browser y confirmar que el logo del Hero es visiblemente más pequeño.

- [ ] **Step 3: Commit**

```bash
git add app/src/components/Hero.tsx
git commit -m "Reduce hero logo size to 400px"
```

---

## Task 2: Quitar números de todas las secciones

**Files:**
- Modify: `app/src/components/Services.tsx:56`
- Modify: `app/src/components/Team.tsx:45`
- Modify: `app/src/components/Gallery.tsx:45`
- Modify: `app/src/components/Booking.tsx:12`
- Modify: `app/src/components/Reviews.tsx:42`
- Modify: `app/src/components/Contact.tsx:13`

- [ ] **Step 1: Services.tsx — quitar `01 · `**

Cambiar:
```tsx
01 · Servicios
```
por:
```tsx
Servicios
```

- [ ] **Step 2: Team.tsx — quitar `02 · `**

Cambiar:
```tsx
02 · El Equipo
```
por:
```tsx
El Equipo
```

- [ ] **Step 3: Gallery.tsx — quitar `03 · `**

Cambiar:
```tsx
03 · El Espacio
```
por:
```tsx
El Espacio
```

- [ ] **Step 4: Booking.tsx — quitar `04 · `**

Cambiar:
```tsx
04 · Reservas
```
por:
```tsx
Reservas
```

- [ ] **Step 5: Reviews.tsx — quitar `05 · `**

Cambiar:
```tsx
05 · Reseñas
```
por:
```tsx
Reseñas
```

- [ ] **Step 6: Contact.tsx — quitar `06 · `**

Cambiar:
```tsx
06 · Contacto
```
por:
```tsx
Contacto
```

- [ ] **Step 7: Commit**

```bash
git add app/src/components/Services.tsx app/src/components/Team.tsx \
        app/src/components/Gallery.tsx app/src/components/Booking.tsx \
        app/src/components/Reviews.tsx app/src/components/Contact.tsx
git commit -m "Remove numeric prefixes from all section labels"
```

---

## Task 3: Botón "Cómo llegar" en Encuéntranos

**Files:**
- Modify: `app/src/components/Contact.tsx:83-85`

`MAPS_URL` ya está importado en Contact.tsx (línea 4): `'https://maps.app.goo.gl/DWMwgH6QEkesm1Rp8'`

- [ ] **Step 1: Reemplazar botón en Contact.tsx**

Cambiar las líneas 83-85:
```tsx
<a href={BOOKING_URL} target="_blank" rel="noopener noreferrer" className="block mt-6">
  <ShimmerButton>Reservar cita</ShimmerButton>
</a>
```
por:
```tsx
<a href={MAPS_URL} target="_blank" rel="noopener noreferrer" className="block mt-6">
  <ShimmerButton>Cómo llegar</ShimmerButton>
</a>
```

- [ ] **Step 2: Limpiar import BOOKING_URL si ya no se usa en Contact.tsx**

Revisar línea 4 de Contact.tsx. `BOOKING_URL` aparece solo en el botón que acabamos de cambiar. Actualizar el import:
```tsx
import { MapPin, Clock, Phone, Instagram as InstagramIcon, ExternalLink } from 'lucide-react'
import { ShimmerButton } from './magicui/shimmer-button'
import { LogoMark } from './LogoMark'
import { INSTAGRAM, IG_URL, MAPS_EMBED, MAPS_URL, PHONE, ADDRESS, HOURS } from '@/lib/brand'
```
(Se elimina `BOOKING_URL` del import de brand.)

- [ ] **Step 3: Commit**

```bash
git add app/src/components/Contact.tsx
git commit -m "Replace 'Reservar cita' with 'Cómo llegar' linking to Google Maps in Contact section"
```
